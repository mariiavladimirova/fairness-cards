# Copy to .env and fill in for your environment.

# Absolute path to the locally downloaded Qwen2.5-7B-Instruct checkpoint.
MODEL_DIR=/path/to/Qwen2.5-7B-Instruct

# Optional: Hugging Face cache root (defaults to ~/.cache/huggingface).
# HF_HOME=/path/to/hf-cache
